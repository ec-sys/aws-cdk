server:
  port: ${SERVER_PORT:8080}
management:
  endpoints:
    web:
      exposure:
        include:
          - health
          - prometheus
    enabled-by-default: 'false'
  endpoint:
    health:
      enabled: 'true'
      show-details: always
    prometheus:
      enabled: 'true'
      show-details: always